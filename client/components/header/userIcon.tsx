import * as React from 'react';

interface IUserIconProps {
}

const UserIcon: React.FunctionComponent<IUserIconProps> = (props) => {
  return (
    <div className='bg-amber-400 w-6 h-6 rounded-full'>

    </div>
  );
};

export default UserIcon;
